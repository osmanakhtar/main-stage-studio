# Brand / visual

**Status: BLOCKED.** Needs the Higgsfield credit top-up decision (`mss-social-content-plan.md` §8, open decision) before this pillar can run. Spec'd here so the pipeline is ready the moment that unblocks; do not draft against this prompt yet.

**Job:** keep a feed worth following; portfolio texture. Motion loops, palette studies, quote cards pulled from process-transparency or studio-philosophy posts.

**Typical formats:** abstract 3D-mark motion using the MSS palette (`#F5EFE5` / `#BF6B47` / `#8C4A2F` / `#1C1712`), a reusable quote-card template pulling a line from another pillar's post.

**Source:** fully Higgsfield-generated (motion/stills) or a one-line pull from an already-approved queue post (quote cards). Not drafted from an inbox note the way the other three pillars are.

## When this unblocks

1. Confirm the credit top-up decision.
2. Generate a template/pattern batch: one quote-card template, 2-3 motion loop variants, per `mss-social-content-plan.md` §5.
3. Route quote-card pulls through the same `queue/` lifecycle as the other pillars, tagged `pillar: brand-visual`, `source` pointing at the queue file it was pulled from rather than an inbox item.
