# Main Stage Studio — Decisions Log

*Living document. Add entries when something is locked or changed.*
*Never delete — mark superseded decisions with ~~strikethrough~~ and note the date.*
*Last reviewed: 3 July 2026*
*Upkeep: Claude Code updates this log in-session whenever a decision locks — see Workspace Structure.*

---

## How to use this file

Three sections:

1. **Locked** — confirmed, in use, not up for revision without a reason
2. **Open** — known decisions that haven't been made yet
3. **Flagged** — things that look missing or need a check before they can be locked

Anything that moves from Open to Locked should be dated and noted briefly.

---

## Locked Decisions

### Positioning

| Decision | What's locked |
|----------|--------------|
| ~~Client definition~~ | ~~Creative founders who lead with instinct and identity — people who know what they're building and why, but haven't yet found the words or the form to make it real~~ |
| ~~Client definition (v2)~~ | ~~People with something real to say that hasn't yet found its form. Founders, established businesses that have lost calibration, creatives at any stage. The common thread is the gap between what something is and how it currently shows up.~~ |
| ~~Client definition (v3)~~ | ~~Established small businesses and funded founders with a run-rate problem. Values filter unchanged (intent, not industry).~~ Superseded 4 Aug 2026 by Client definition (v5). |
| ~~Core proposition~~ | ~~"You know what you want. We know how to build it."~~ |
| ~~Core proposition (v2)~~ | ~~"Your vision doesn't need permission. It needs form."~~ |
| Core proposition (v3) | "You don't need a bigger team. You need a better system." Old hero line retired from the hero; lineage preserved in About section copy. Carries forward unchanged into v5. |
| Positioning | Presence partner (build + run a client's brand/site/content on retainer) — recommended over the "AI consultancy" reading of the 25 June stack pivot. The delivery pipeline is the mechanism, not the pitch. Carries forward unchanged into v5. |
| ~~Offer architecture~~ | ~~Launch (build engagement) / Run (ongoing retainer, centre of gravity). Campaign folded into Run rather than named as a third tier.~~ Superseded 4 Aug 2026 — see Offer architecture (v5). |
| ~~Retainer band~~ | ~~New Run proposals anchor at £1,500–£3,000/mo — raised from the plan's original £1,200–£2,500 draft on the view that the delivery system is now proven, not speculative. FSC's existing £400/wk (~£1,733/mo) stays as-is; not renegotiated as part of this decision. Site copy stays qualitative; band used in proposals only.~~ Superseded 4 Aug 2026 for the regulated-services vertical — see Presence Monthly band (v5). This band still applies to any non-vertical Run engagement. |
| PureMed on the MSS site | Off-site lock reversed: PureMed becomes a Work case study on the MSS site, framed as delivered, regulated-industry proof, per `mss-website-copy-v3.md` §8. Client comfort confirmed 5 Aug 2026, founder has the green light to use it as a case study. Engagement itself is still separately parked (21 Jul entry below); that doesn't block showing the delivered work. Dedicated PureMed case study page to be built once the Work page exists. |
| Studio name | Main Stage Studio |
| ~~Sector~~ | ~~Broad — any sector. Not limited to creative founders or any single vertical.~~ Superseded 4 Aug 2026 — see Sector (v5). |
| ~~Values filter~~ | ~~Drawn to founders whose work has positive impact — engagements are chosen accordingly~~ |
| Values filter | Drawn to people whose work means something beyond the commercial. Not sector-specific. The filter is intent, not industry. |
| Differentiator framing | PM rigour as the method, not the message. AI accelerates execution. Vision and judgment stay human. End-to-end: discovery through to live digital presence. |
| Studio voice in one line | Quietly confident, and genuinely empathetic. |
| Three words | Considered. Direct. Distinctive. (Aspirational — chosen with intention, not yet earned in public perception) |
| Personal/public boundary | Thinking is public. Person stays private. |
| AI positioning | AI as a thinking partner and craft tool, not a shortcut. The judgment about what to do with it hasn't changed hands. The live layer of the website is proof of concept for clients. |
| Studio structure | Single studio. No second agency or brand. A second studio concept (provisional working name "Studio 2", execution-first proposition for professional consultants) was explored after 25 May and reversed on 17 June 2026, following a sense check on overhead at pre-revenue stage. PureMed Aesthetics and Locked In Learning are standalone personal projects, not under any studio brand. They may surface later on a future MSS Work page, at the studio's discretion, but are not part of MSS positioning. |
| Vertical bet (v5) | Regulated professional services, founder-credential-led. Locked 4 Aug 2026, superseding v4's unratified "sports flagship, clinics money" bet (23 Jul, never locked). Canon: `01_mss/strategy/mss-positioning-v5-regulated-services.md`. |
| Client definition / Sector (v5) | Broad regulated or trust-sensitive professional services: financial advisers, boutique wealth managers, insurance brokers, accountants, legal practices, regulated fintechs. Deliberately not narrowed to one sub-vertical — the filter is "regulated/trust-sensitive," not a single industry. |
| Flagship proof (v5) | The founder's own career is the flagship credential, not an anonymised client operation: 20+ years product delivery, ~13 years across UK financial services (retail banking, a core-banking platform vendor, consumer credit, now a regulated wealth platform), ~10 years before that in broadcast technology. Employer names never used in MSS marketing (same discipline as the FSC anonymisation rule, applied to the founder's own CV). FSC and PureMed studio work remain secondary "the machine works" proof, not sector-matched case studies. |
| Offer architecture (v5) | Launch / Presence Monthly / Campaign. Presence Monthly renamed from v4's draft "Clinic Monthly"; supersedes v3's "Run" naming for this vertical. |
| Presence Monthly band (v5) | First regulated-services client anchors low as a founding-client rate, **£1,200–£1,800/mo**, intentionally below the general Run band, to land the reference case study rather than price out of it. Moves toward the standard £1,500–£3,000/mo Run band once that first client is delivered and proven, with £2,000–£4,000+/mo as an aspirational ceiling thereafter (not v4's assumed clinic-LTV figure, which doesn't transfer to this buyer set). |
| Site register (v5) | Credential-led proof-of-operation. Homepage order: Hero → The gap → **Who's behind it** (moved up, ahead of the machine demo) → What we do → See the machine → The promise → Work → Start. Draft "Who's behind it" copy in `mss-positioning-v5-regulated-services.md` §10. |
| Vera POC / clinic content model | Parked 4 Aug 2026. The pipeline capability it proved (Studio, Stage, compliance-lint pattern) still stands as general proof; the clinic-specific content pillars and campaign model don't carry forward to the regulated-services ICP. The shared-renderer light/bone card fix ([[project-vera-poc]]) is no longer a blocking priority. |
| Layer 2/3 (self-serve product tier, flagship-as-revenue) | Stay deferred (carried from v4, reconfirmed 4 Aug 2026). Not built or designed toward until a first regulated-services client exists to design them from. |
| Systems service line (v5 §9.9) | Locked 4 Aug 2026. MSS runs as **two service lines**: Presence (Launch / Presence Monthly / Campaign, packaged, unchanged) and **Systems** (new), config-driven, deterministic, potentially bespoke operational architecture for regulated firms, client/case journey design, policy-gate logic, onboarding/intake workflow tooling. Systems is scoped and quoted per engagement, not packaged, and sells on the founder's personal credential alone until a first engagement exists, the closest real proof (the founder's own wealth-onboarding journey architecture) can never be shown publicly, same anonymisation boundary as everything else. Higher delivery stakes than Presence; needs a scope-of-responsibility contract clause before it is actively sold. Canon: `01_mss/strategy/mss-positioning-v5-regulated-services.md` §5.2, §8 risks 5-6. |

---

### Brand Identity — Visual

| Decision | What's locked |
|----------|--------------|
| Palette — Parchment | `#F5EFE5` — base / background / large surfaces |
| Palette — Warm blush | `#E8C9AE` — cards / secondary sections / subtle texture |
| Palette — Dusty terracotta | `#BF6B47` — primary accent / CTAs / logo mark / key moments |
| Palette — Deep ember | `#8C4A2F` — accent hover / depth |
| Palette — Near black | `#1C1712` — all type / structural elements / primary buttons |
| Colour principles | Max one accent colour per composition. Parchment is default background — never stark white or cool grey. Near-black for body type — never pure black. Terracotta as punctuation, not wallpaper. |
| Logo mark — structure | Two offset squares with deliberate negative space at intersection. Near-black top-right (leads, on top). Terracotta bottom-left (recedes behind). Parchment cutout at intersection. |
| Logo mark — corner radius | 2.5px on both squares, 1.5px on cutout |
| Logo mark — dark bg variant | Near-black square uses `#3D3128` to remain visible against dark backgrounds |
| Wordmark — Main Stage | Cormorant Garamond, weight 400, letter-spacing 0.02em |
| Wordmark — Studio | Plus Jakarta Sans, weight 400, letter-spacing 0.24em, uppercase, terracotta |
| Flat mark usage | Always primary in functional contexts — favicon, header, print, documents, email signatures |
| 3D mark / Higgsfield usage | Expressive layer only — hero video, brand film, social content. Never replace flat mark in functional contexts. |
| Minimum mark size | 32px |
| Typography — primary | Cormorant Garamond — display headings, wordmark, editorial moments. Weights 300–400 only. |
| Typography — secondary | Plus Jakarta Sans — body copy, navigation, UI labels, sub-headings. Weights 300–500. |
| Typography — Inter | Never use. Too generic for this identity. |
| Typographic principles | Headlines: large, tracked tight, left-aligned. Body: 16px, relaxed line-height, max-width ~65ch. All caps only for labels/descriptors at small sizes with wide tracking. No centred hero bias. |

---

### Brand Identity — Aesthetic

| Decision | What's locked |
|----------|--------------|
| Style direction | Editorial motion — considered, typographically confident layouts. Kinetic moments that hit hard because surrounding space is calm. |
| Motion principles | Hardware-accelerated only — `transform` and `opacity`. Never `top/left/width/height`. Motion is punctuation, not sentence. |
| What to avoid | Centred hero layouts. Generic card overuse. Purple/blue AI aesthetic. Gradient backgrounds. Motion as decoration. |
| Identity system — Layer 1 (Static) | Flat SVG mark + wordmark + palette + typography. Use for: PDF proposals, documents, favicons, email signatures, headers. |
| Identity system — Layer 2 (Live) | 3D mark render + Higgsfield video + scroll-triggered motion + kinetic typography. Use for: website hero, case study openers, social content, brand films. |

---

### Generative Assets

| Decision | What's locked |
|----------|--------------|
| Higgsfield hero asset | Generated and locked. MSS homepage hero asset, produced once visual identity was fully locked and the creative brief was clear. Closes the earlier "can only generate once identity locked" open item. Confirmed 20 June 2026. |
| Logo mark colour meaning, corrected | 6 Aug 2026: `mss-brand-identity.md`'s mark description had the symbolism backwards. Black is MSS's own structure/systems, invisible support, "a shadow that supports the client." Terracotta is the client, elevated and made visible. Not "black = vision." Position description (black leads/top-right, terracotta recedes/bottom-left) was always correct and is unchanged. |
| Higgsfield Gap + What We Do + How It Works assets | Generated and locked, 6 Aug 2026. Three placements: Gap section anchor image, shared texture behind the Retainer + Systems cards, and a dark-background variant of the same motif in the How It Works left column, deliberately not one image per card, per the "no generic card overuse" principle. Seedream 4.5, same offset near-black/terracotta motif as the logo mark throughout, no new visual language introduced. The Gap and How It Works anchors both get a single quiet scroll parallax (±12px translateY, transform-only) — not a loop, not a hover state. Cost 3 credits total for the three stills. Bonus: a 5s abstract motion loop in the same motif generated for the social content plan (22.5 credits), saved to `01_mss/marketing/assets/ig-brand-loop-v1.mp4`, not used on the site. |

---

### Tone of Voice

| Decision | What's locked |
|----------|--------------|
| Brand character | Quietly confident, and genuinely empathetic. The rigour is real. So is the warmth. Neither works without the other. |
| Non-conformist thread | The confidence in the voice has a spine. A belief that the vision deserves to be heard regardless of where it comes from or what structures it has to navigate. This doesn't appear on the surface. It runs underneath everything. |
| What it never sounds like | Fluffy (no passion without substance). Arrogant (no "most studios do it wrong"). Unsure (no excessive hedging). Corporate (no passive voice, no buzzword stacking). |
| Copy principles | Say what you mean. No filler. Active voice. Lead with the reader's world, not the studio's process. Endings close with weight. Empathy before capability. |
| Five copy contexts | First impression / Discovery framing / Case study / Editorial / Proposal — each with distinct register. See `mss-tone-of-voice.md` and `copy-contexts.md`. |
| Empathy layer | Added 02 June 2026. The voice shift: same rigour, more warmth. Same clarity, more expressiveness. The studio understands what it feels like to have a vision you haven't found the words for. That's not stated as autobiography — it comes through in how the studio listens and works. |

---

### Skills

| Decision | What's locked |
|----------|--------------|
| Copywriting skill | Built and active. Two layers: craft (constant) and voice (context-dependent). Lives in `.claude/skills/copywriting/SKILL.md`. |
| Creative director skill | Active. Applies across all MSS and client sessions. |
| Voice files | MSS work uses `mss-tone-of-voice.md`. Client work uses the client's own tone of voice file. No copy before the voice file is loaded. |
| Bricks skill system | Added 19 June 2026. The original single `bricks-html-importer` skill was being violated in production (Code elements used for content where native elements should have been used) and mixed three separate concerns. Replaced with four skills: `bricks-site-connection` (MCP and environment setup), `bricks-lowcode-skill` (element and style reference catalogue), `bricks-html-importer` (HTML-to-Bricks conversion workflow), and `bricks-mss-site-notes` (MSS-specific build history and fixes). See Flagged for a verification gap in the element catalogue. **Update 3 July 2026:** Bricks retired for new builds (Astro pivot, 25 June). The four skills stayed installed for legacy live-site fixes only until the MSS production site's Astro cutover. **Update 5 August 2026:** the MSS production site now runs on Astro (see Website > Domain and the 22 July Change History entry) — the four skills are legacy-only and can be archived. |
| Higgsfield asset generation skill | Added 22 June 2026. Generates a full cross-section of image and video assets for a client site in a single job. Reads the HTML prototype and brand documents, maps every placement, writes generation briefs grounded in brand and constraints, selects models, runs all jobs via the Higgsfield MCP, and produces a placement manifest in the client folder. Lives in ~/.claude/skills/higgsfield-asset-generation/ alongside the other global skills. Requires Higgsfield MCP connected at https://mcp.higgsfield.ai/mcp — add to global Claude Code MCP config before first use. Plus plan free image models (Flux.2 Pro, GPT Image, Seedream 4.5, Kling O1 Image, Nano Banana, Seedream 5.0 Lite) are described in the skill's model guide as free with a "free toggle," but on the account in use as of 6 Aug 2026 the MCP's `use_unlim` flag is a separate free-trial allowance and was rejected outright (not silently charged); Seedream 4.5 actually cost 1 credit/generation on this account, trivial at scale but the guide's "no credits drawn" claim did not hold — verify actual cost via `get_cost` rather than assuming free. Video generation draws credits — reserved for hero placements only. |

---

### Website

| Decision | What's locked |
|----------|--------------|
| ~~Stack~~ | ~~HTML/CSS prototype (Phase 1) then Bricks Builder on WordPress (Phase 2)~~ Superseded 25 June 2026 — see next row |
| Stack | HTML/CSS prototype (Phase 1) then Astro static site (Phase 2), deployed to Cloudways. Decided 25 June 2026. Bricks/WordPress retired after silent platform bugs (header meta-key sync, global settings deletion, ~2KB MCP payload truncation), two-layer caching, and the LocalWP dependency. Astro gives deterministic build pass/fail and no WordPress maintenance surface. Setup guide: `01_mss/strategy/mss-astro-cloudways-setup.md`. Legacy docs: `_archive/bricks-era/`. |
| Cloudways/CI pipeline — live | `mss-static` PHP app created on `mss-do-lon-01` (app ID `1634277`, system user `pvymeytcaq`, webroot `/home/1634277.cloudwaysapps.com/pvymeytcaq/public_html/`). Confirmed 22 July 2026: `github_actions_mss` deploy key added via the server-level **Master Credentials → SSH Public Keys** panel (master login `master_azaewextjh` — this Cloudways generation has no per-application public-key upload, only a password-only "Add SFTP User" modal, so the setup doc's assumption of a server-wide master SSH login was correct and is the only path). `CLOUDWAYS_SSH_KEY`/`HOST`/`USER`/`WEBROOT_STAGING` set as repo secrets on `osmanakhtar/main-stage-studio`; `CLOUDWAYS_WEBROOT_PROD` deliberately left unset until domain cutover. `mss-site-staging.yml` verified fully green end-to-end (build → rsync → live page confirmed at the temporary Cloudways URL). Staging subdomain not added — the free `phpstack-1634277-6570410.cloudwaysapps.com` URL is sufficient per the migration plan, no DNS work needed yet. Workflow doc: `.github/DEPLOY.md`. |
| Client review | Stage — self-hosted review and sign-off tool on the Pi (`mss-review.duckdns.org`). Clients review prototypes and sign off; sign-off data (manifest `astroFile`/`astroProp` per copy section) feeds the apply step. Live since late June 2026; PureMed is the first engagement. |
| Hosting | Cloudways |
| Version control | GitHub — osmanakhtar/main-stage-studio |
| CSS framework | Tailwind CSS |
| Code principles | Asymmetric, editorial. No centred hero. No generic card overuse. Hardware-accelerated motion only. `min-h-[100dvh]` not `h-screen`. No emoji, no gradient backgrounds. |
| Domain | `mainstagestudio.co.uk`. Live and published as of mid-June 2026, originally hosted on Cloudways via WordPress and Bricks. **Correction 7 August 2026:** the 5 August note below claiming the live site already ran on Astro was premature, the domain was still on WordPress (confirmed via DNS and response headers) until the actual cutover, done 7 August 2026 — see Change History. The domain and its `www` alias now point to the `mss-static` Cloudways app; the WordPress app (`mainstagestudio`) no longer claims the domain and is reachable only at its temporary Cloudways URL. ~~A previous version of this log incorrectly recorded the domain as `mainstagingstudio.co.uk` — confirmed and corrected 20 June 2026.~~ |
| Workflow | Phase 1 (Claude web/desktop): discovery, positioning, copy, creative direction, HTML/CSS prototype. Phase 2 (Claude Code): build, GitHub push, publish. |
| Work section | Homepage shows curated highlights — two or three strongest case studies. Dedicated Work page holds full portfolio. "View all work" link on homepage. Work page not built until three case studies are fully complete, and deferred further behind the PureMed Aesthetics build per the 17 June studio structure decision above. |
| DAM | Bunny.net account provisioned but parked. Asset compression to WebP via the Higgsfield generation pipeline brings file sizes small enough that Cloudways direct serving is sufficient for brochure sites. DAM revisited only when a client requires significant asset management. Confirmed 23 June 2026. |
| ~~Bricks Builder licence~~ | ~~Lifetime, one-time fee. Purchased and confirmed, resolving the earlier annual-vs-lifetime open decision.~~ Cancelled and refunded 27 June 2026 as part of the Astro pivot. |
| Contact form | Native WordPress, custom must-use plugin (`mss-contact.php`) relaying through Google Workspace SMTP to hello@. Reply-To set to the visitor's email. Resolves the earlier native-vs-third-party open decision. See Flagged — one session noted the live contact section rendering as text only with no visible form, needs confirming. |
| Bricks layout element for layout wrappers *(legacy — Bricks retired, see Stack above)* | **Container**, not Div. Confirmed 21 June 2026: live MSS homepage (then on Bricks) had 33 Container elements and 2 Div elements. The 2 Divs were the empty atmospheric-image overlay elements in dark sections — they stayed as Div (they're purely positioned, no flex/direction controls needed). All other layout wrappers inside sections were Containers. The old Div-only rule in `mss-bricks-build-guide.md`'s checklist was corrected. All four Bricks skills (`bricks-lowcode-skill` Rule 1, `bricks-html-importer`, `bricks-mss-site-notes`) consistently said Container. No equivalent decision needed for Astro/Tailwind. |

---

### Case Studies

| Decision | What's locked |
|----------|--------------|
| Case study structure | Each case study has a framing page before the full write-up. The framing page is written as part of the story phase, not the build phase. Added 02 June 2026. |
| Self-initiated concepts | Positioned transparently as self-initiated briefs, not fabricated client relationships. One honest line in the case study resolves the framing. |
| Portfolio, entry level | ~~MSS building MSS and Sable lead the homepage grid.~~ Superseded 7 Aug 2026. The Work section now runs PureMed as the feature card (regulated web build), with Sable and Ayesha in a two-up row beneath it. MSS-building-MSS is not shown: the whole site is that case study. |
| Portfolio — depth | Apex FC and a professional services concept (doctor, consultant, dietitian) were planned for the dedicated work page. Apex FC case study copy is paused, not active — may be picked up later, possibly as proof of capability on a larger site. The professional services concept is also on hold, see Open Decisions. |
| Portfolio — off site | ~~PureMed and Locked In Learning stay off the MSS site for now.~~ PureMed reinstated as a Work case study 5 Aug 2026 (client comfort confirmed) and promoted to a full client engagement 6 Aug 2026. Locked In Learning stays off site: standalone personal project, not part of the MSS proposition, may surface later on the future Work page at the studio's discretion. |
| Creative identity rule | Each case study has its own creative identity — not inherited from MSS brand. If it starts looking like the MSS website, stop and restart from the brief. |
| Sable case study | Live on the Astro site at `/work/sable` as of 7 Aug 2026, ported from the standalone `01_mss/portfolio/sable/case-study.html`. The WordPress hidden page is dead history. Sable's own site ported the same day to `/work/sable/site`, so the closing "enter" works: case study to artist site to case study is a real loop. The artist site is `noindex` and carries a permanent "prototype, invented artist" return bar. |
| Ayesha case study | Homepage card is back on the Astro site as of 7 Aug 2026, marked "In progress" with a footer note reading "Web build to follow, late 2026". Deliberately not a link and deliberately not a spotlight card, so nothing on it implies a page that does not exist. Underlying engagement status: brand strategy document delivered and currently with the client for review. Not actively being chased , deprioritised behind internal work and the PureMed build. |
| Portfolio folder naming | Resolved 20 June 2026. Workspace taxonomy confirmed: `01_mss/portfolio/` contains `apex-fc`, `mss`, and `sable`. Earlier folder names (`chef`, `music`) no longer exist. |

---

### Workspace Structure

| Decision | What's locked |
|----------|--------------|
| Repo root | `~/workspace/main-stage-studio/` — public GitHub repo |
| Client projects | Live at `02_clients/` — never inside `01_mss/` |
| Case studies | Live at `01_mss/portfolio/[client]/` — not in the client folder |
| mp4 assets | Local only — excluded from git. Hosted via Bunny.net once live. |
| ~~`locked-in-learning/admin`~~ | ~~Local only — never pushed to GitHub~~ — ~~migrated to Studio 2 (25 May 2026)~~ — Studio 2 reversed 17 June 2026, see Positioning |
| Locked In Learning | Standalone personal project. Sits in its own top-level workspace folder, separate from the MSS repo. Not part of MSS or any studio brand. |
| PureMed Aesthetics | ~~Standalone personal project, same status as Locked In Learning.~~ **Promoted to a full MSS client engagement 6 August 2026** and moved to `02_clients/puremed/`, with its publish worktree at `02_clients/puremed-publish/`. It keeps its own git repo (`osmanakhtar/puremed-aesthetics`) and deploy workflows, so both directories are gitignored by the Studio repo, the same convention the workspace repo uses for `main-stage-studio/` itself. Working files stay there; the case study is separate, per the case-study rule above. |
| Each case study | Has its own creative identity — not inherited from MSS brand |
| Two `.claude` folders | `~/workspace/main-stage-studio/.claude` is the repo-specific context folder: MSS strategy, brand, voice, decisions. `~/.claude/` (osmanakhtar/.claude) is the global layer: portable skills and Claude Code config that aren't tied to MSS. Skills that are genuinely cross-project (Bricks skill family, copywriting, creative-director) live globally. Anything that would be wrong or missing if a session forgot MSS context lives in the repo folder. Confirmed 19 June 2026 via README audit. |
| Decisions log upkeep | This log stays Layer 0 canon and Claude Code maintains it: whenever a decision locks, reverses, or resolves in a session, Claude updates this file in that same session — auto-memory is the capture layer, not the record. Decided 3 July 2026 after the log was found stale against the Astro pivot (memory-only management was considered and rejected: memory is invisible to Claude desktop, to sessions in other working directories, and to headless agents). Standing rule saved to Claude auto-memory (`feedback-decisions-log-upkeep`). |

---

### Committed Subscriptions

| Tool | Cost | Status |
|------|------|--------|
| Claude Pro | £20/month | Committed |
| Higgsfield | $45/month | Extended for a second month. Current period: 18 June – 18 July 2026, on a month-by-month basis. Review again at the end of the current period. |
| ~~Bricks Builder~~ | ~~Lifetime licence~~ | ~~Purchased and confirmed. Covers MSS, Ayesha, and the personal projects (PureMed, Locked In Learning) as shared infrastructure only, not a shared brand.~~ Cancelled and refunded 27 June 2026 — Astro pivot. |
| Constraints template | `02_clients/_template/discovery/constraints.md` — confirmed present and canonical as of 20 June 2026. Placed during 12 June migration, paths corrected and closing specificity-rule "Notes" section added on 20 June merge with the strategy-folder draft. |

---

## Open Decisions

These are known — they haven't been made yet. Some are blockers.

| Decision | Why it's open | Blocker? |
|----------|--------------|---------|
| LinkedIn company page | **Partially resolved 18 Aug 2026.** Posting surface: personal profile leads, company page is a slower-moving mirror. Content pipeline built at `01_mss/marketing/content-engine/` (SOP-MSS-003), scoped to page-appropriate pillars only (Process transparency, Case study/proof, Studio philosophy; Brand/visual blocked on Higgsfield credits), MSS-only for now (not built as a reusable client pattern), manual posting (no LinkedIn API integration). Still open: whether to generalize into a reusable pattern once proven, and the Higgsfield credit top-up. | Tied to hard launch |
| Spend tracker reconciliation | Additional Claude spend and subscription costs to be logged | Not a decision — see Action Items below |
| Professional services case study | Doctor, consultant, dietitian or similar. Concept brief to demonstrate that use case. Blocked behind a more fundamental capability gap: a confident, repeatable pattern for spinning up a new site ~~in Bricks~~ (now Astro, per the 25 June pivot) usable as a white-label example. PureMed is the proving ground for that pattern. This case study doesn't move until PureMed does. | Blocked on PureMed Astro pattern |
| Work page structure | When the Work page is eventually built: will it replace the homepage's curated highlights, hold full case studies directly, or link out to separate framing pages. To be settled once the PureMed build is complete and the Work page is actually being designed. | Deferred by design |

---

## Action Items (not decisions)

Things that need doing, not deciding. Tracked here so they don't get lost, but they don't belong in Open Decisions.

| Item | What needs doing |
|------|-------------------|
| Spend tracker reconciliation | Update `mss-spend-tracker.md` with additional Claude spend and subscription costs as they're confirmed. Ad hoc, ongoing. |
| Full sprint tracker review | Founder is reviewing each context document in turn as part of this session's broader pass — no decision required, just completion. |

---

## Flagged — Needs Check

Items that look incomplete or potentially contradictory. Resolve before treating them as locked.

| Item | What needs checking |
|------|---------------------|
| `mss-website-brief.md` | Proposition line and hero copy updated in session 05 June 2026 — file is stale against current locked copy. Fix deferred until the next homepage copy pass; reconciliation prompt prepared, not yet run. |
| `mss-tone-of-voice.md` first impression example | Still references "creative founders." Fix prompt issued 20 June 2026 (replace with "people") — pending local execution and confirmation. **Resolved 3 July 2026:** tone-of-voice file was already clean; the stale phrase actually lived in `copy-contexts.md` (worked example) and `mss-brand-identity.md`, which also still carried the retired core proposition and values filter. All three corrected to match locked positioning. |
| `mss-tone-of-voice.md` and `copy-contexts.md` — audience reference consistency | Audit needed for consistent use of "the client" / "the reader" / direct second person, outside of worked copy examples (where second person addressing the client is expected and correct). Audit prompt prepared 20 June 2026, not yet run. |
| MSS case study | Needs full rewrite through updated lens — non-conformist thread, wider client definition, AI as thinking partner framing. On hold until the copy document is updated and tone of voice is confirmed reflective of current positioning. |
| Contact form frontend | Backend (mu-plugin and SMTP relay) is built, but one session noted the live site showing the contact section as text only, no visible form rendering. Confirm the form actually renders before treating this as fully resolved. |
| `mss-new-client-checklist.md` | Not yet reviewed in this review cycle — may be out of date |
| `mss-new-case-study-checklist.md` | Not yet reviewed in this review cycle — may be out of date |
| `mss-spend-tracker.md` | Not yet reviewed in this review cycle — update ad hoc when figures confirmed |
| `mss-bricks-build-guide.md` / `mss-bricks-quick-fixes.md` | Diff run 20 June 2026 confirmed the two files serve genuinely different purposes: build guide is a workflow doc (planning phase, session openers, build order, prompt templates, critical rules); quick-fixes is a symptom lookup table (UI fixes, reached mid-session when something breaks). Do NOT merge. Remaining blocker: stale skill path — build guide's final line points to `~/.claude/skills/bricks-html-importer/` (old single-skill location), not the four-skill structure. Full diff against the skill's content still needed to confirm what, if anything, these files cover that the skill doesn't. Div/Container contradiction resolved 21 June 2026 — see Locked. **Resolved 3 July 2026:** both files archived to `_archive/bricks-era/` with the Astro pivot — the diff is no longer needed. |
| `bricks-lowcode-skill` element catalogue | Added 19 June 2026. Built from a live `get_builder_guide` pull against PureMed's install, not MSS's own. Needs a fresh pull against the MSS/Ayesha install before its element list can be treated as verified for MSS work. **Moot 3 July 2026:** Bricks retired for new builds — verification only matters if a legacy live-site fix ever requires it. |
| Docs audit | Confirmed present at repo root (docs-audit-2026-06-19.md), tracked in git since commit b7e1e31. Of its 10 numbered decisions: closed are 1 (build-guide duplicate deleted), 2 (all three HTML files deleted 20 June: MSS_Architecture_Design_v0.3.html content extracted to mss-system-architecture.md and mss-tooling-rationale.md; mss-build-guide.html Section 9 merged into mss-go-live-checklist.md before deletion; mss-operating-manual.html deleted, was never git-tracked, fully superseded by current context files), 4 (ghost file investigated, no action needed), 5 (strategy-folder duplicates resolved), 6 (copywriting skill consolidated), 7 (bricks-mss-site-notes now points to the build guide and quick-fixes files), 8 (mss-production-ops.md created), 10 (CLAUDE.md corrected). Still open: 9 (final sweep of this log for stale Open items — largely done through 20 June fixes, worth one more pass to confirm). Closed: 3 (Bricks_Workflow_User_Guide.docx deleted 20 June — format violated markdown-only convention, content superseded by four-skill system; session ordering and extension protocol preserved in README.md before deletion). |
| Project custom instructions | Recommended 13 June 2026: move context files into Claude Project custom instructions and knowledge instead of attaching per session, plus a model-routing default (Haiku for mechanical work, Opus for thinking). Adoption status unconfirmed. |
| `mss-go-live-checklist.md` | Marked v0.1, never validated against a real go-live. The MSS site has been live for weeks — review this checklist against what the actual go-live required and update or confirm it. **Resolved 3 July 2026:** archived to `_archive/bricks-era/` — the LocalWP → Bricks go-live flow it describes no longer exists. Astro deployment gets its own checklist once GitHub Actions is live. |

---

## Change History

| Date | What changed | Why |
|------|-------------|-----|
| 10 August 2026 | Home page: hero reaches the Systems half, Work section reordered around proof, Booking Engine clip re-recorded against the current prototype | Founder review of the local build. **Hero** carried three paragraphs of presence (site, content, words) and never reached the conversion argument the rest of the page is built on; rewritten to presence, then the gap between interest and a paying client, then the studio building both. **Work section** led with PureMed and explained its own layout ("written up properly, rather than a grid of thumbnails" describes the page, not the work). Reordered so the two things a stranger can inspect lead as full-width features (Self-Serve Onboarding, then Sable, promoted from the small row), with PureMed and Ayesha following under a "Current engagements" heading as a smaller two-up. PureMed's thumbnail moved onto its own primary blue (`#23476A`, `puremed-brand-identity.md`) rather than near-black, and its copy now states outright that it is a regulated business. All four cards share one border (three different values were in use) and one 16:9 thumbnail. The dead band under the featured thumbnails is absorbed by the thumbnail's own panel, which fills the card height with the 16:9 still centred in it: the image is never stretched to close a gap. **What we do:** the `Systems · Convert` eyebrow named what we want the visitor to do, and is now `Systems · Booked`, which names what they get; the card's photographic texture is now the flat `#EAE0D0` its wash was resolving to anyway; and the Presence · Ongoing card, roughly twice the text of the Launch card beside it, is now two matched paragraphs. **Contact form** lost the "What are you looking for?" select, which made a visitor categorise themselves before saying anything the free-text field does not already carry. **The clip** was recorded against a prototype UI that has since moved, so it is now produced by a committed script (`tools/clips/record-onboarding.js`) driving the published prototype over HTTP: 40s at 25fps against the old 61s at 8fps, with a visible cursor and a caption after each decision point naming the rule that fired and the condition that satisfied it. Caption text is read out of the live rule trace in the page and the run throws if a rule it was asked for never fired, so a caption cannot claim a rule the engine did not apply. The Work thumbnail is captured by the same script. **Open:** PureMed's blue is taken from the MSS rebuild spec, which `puremed-brand-identity.md` itself flags as unreconciled with the live dermis.ai palette (`#343a67`) — if that reconciliation lands the other way, the tile needs updating. |
| 10 August 2026 (fourth pass) | Clip trimmed 3s off the front; poster is now the clip's own first frame | Founder. The lead-in was the load settle and the click that puts the studio dock away: worth having on the capture, since it shows the dock docking itself is the prototype's behaviour rather than something the recorder faked, and dead air on a homepage clip. Trimmed at encode time from the raw capture (`-ss 3`) rather than by re-encoding the shipped mp4, so it costs no quality: 74.5s and 4.8MB. The poster is now extracted as frame 0 of the file that ships, so the still and the first frame are the same image **by construction** instead of by a hand-picked timestamp that goes stale the next time the cut is retimed, which is exactly how they drifted apart in the first place. Both steps are in the clips README encode block. |
| 10 August 2026 (third pass) | Clip opens on the accountancy practice with no tenant toggle; prototype gains first-run coach marks and `?tenant=` / `?coach=0` | Founder: the toggle from conveyancing to accountancy is not what the first clip is proving, and the prototype gives a visitor no idea how to start. **Clip** now loads `?tenant=marbury&coach=0` and opens on the practice site directly, 78s. **Coach marks:** three tips appear in turn on first load and take themselves away, pointing at the three things a visitor cannot guess from a convincing practice website: where the journey starts, that the rule trace exists, and that the switcher behind the dock has three practices in it. Deliberately not a modal tour: no overlay, no next button, nothing to dismiss, and any interaction cancels the rest, on the principle that someone who has started clicking does not need to be told where to click. **`?tenant=` earns its place beyond the recording:** it lets a link land a prospect on the practice that looks like theirs instead of on a conveyancer plus a switcher they have to find, and it is now in SOP-BOOK-001. **Fork found and closed:** the published prototype at `site/public/work/booking-engine/prototype/index.html` had been edited directly between 8 and 10 August while `booking-engine/prototype/` was not kept in step, leaving canon 286 lines behind what the site actually served (still carrying the retired sticky shell-head instead of the dock), with SOP-BOOK-001 pointing operators at the stale file. Published copy snapshotted to `booking-engine-prototype-v1.4.html`, plan and SOP updated to say the site copy is the one that ships and this direction of copying is the only supported one. Two stale SOP steps fixed while there: the tenant switcher and Start again have been in the dock bottom right since the header was retired, not top right. |
| 10 August 2026 (second pass) | Clip recut as a narrated demo at full resolution; Work thumbnails stop letterboxing | Second founder review of the same build. **Resolution:** the clip was recorded at 1080 and written down to 720, so a resample sat on top of VP8's own loss and the widget copy came out grainy. Now captured and delivered at native 1920x1080 with no scaling pass, CRF 20. That is 5.7MB for 83s; CRF 23 (4.1MB) and 1600x900 at CRF 21 (3.7MB) were both measured and rejected, because softening the UI copy defeats the clip, and it is `preload="none"` click-to-play so nobody pays for it unless they ask. **Rule trace docked.** It is the compliance artefact and it stays in the prototype, but on a first-viewing demo it is a wall of small type competing with the journey. **Narrative panel** takes the ~575px of empty page left of the 720px centred widget: same position every beat, explaining what the system is validating and orchestrating rather than reprinting the trace. The prose is authored for the first time here; what is *not* authored is the claim that a rule fired, since every beat still looks its rule up in the live trace and the run throws if it is absent, and the footer prints the engine's own condition string verbatim. **Pace:** 40s read as rushed, so each panel's hold is now derived from its own word count and the cut runs 83s. The first narrated draft ran 162s, which was a copy-length problem, not a pacing one: it was cut to ten beats at roughly twenty words each, with the overseas and clearance answers now given without a line, because the four risk questions make two points between them and were restating them four times. **Poster** now comes from a frame with the panel showing, so the static screen and the clip are visibly the same journey, and the Work thumbnail is that same journey on the documents step. **Work thumbnails:** no letterboxing anywhere. The featured split went 2fr/3fr to 1fr/1fr and the images cover their panels. The bug behind the worst of it: Sable's still is square and the image was in normal flow, so it set the grid row height and rendered that whole card as an exact square. Images are now absolutely positioned, so the row is sized by the copy alone; both featured panels land at 1.38:1 and the engagement tiles at 1.6:1. |
| 7 August 2026 (go-live) | MSS site cut over to production: DNS/domain now points at Astro, WordPress fully retired from `mainstagestudio.co.uk` | The Work section rebuild (Sable + PureMed case studies, three-engagement layout) and the 4 August regulated-services repositioning had been built and committed locally but never pushed or deployed — `git status` on `main-stage-studio` showed 27+ modified files and 30+ new files sitting uncommitted, and the last commit actually on `origin/main` was from 25 July. Committed and pushed (`44c1be7`), which triggered the staging pipeline automatically. **Bug caught before go-live:** the homepage's Work cards and Sable's internal nav linked to `/work/puremed`, `/work/sable`, `/work/sable/site` with no trailing slash; Cloudways' nginx 301s those to the directory form but does so using the scheme it sees internally (HTTP, since SSL terminates upstream of nginx) rather than what the visitor requested, and there's no HSTS to catch the fall — every case-study click would have silently downgraded to an insecure connection. Fixed by adding trailing slashes to all four links (`715c091`), rebuilt, and force-purged Cloudways' Varnish cache (server-level, `Manage Services → Varnish → Purge`) since Varnish kept serving the pre-fix homepage after the first deploy despite a clean rsync. Ran `mss-site-deploy.yml` (`CLOUDWAYS_WEBROOT_PROD` had been deliberately left unset since 22 July — set it to the same webroot as staging, since production and staging are the same Cloudways app, differentiated only by which domain nginx routes to it). **Cutover itself was not a registrar/DNS change:** `mainstagestudio.co.uk` already resolved to `mss-do-lon-01`'s IP (the WordPress app and `mss-static` share the one server), so cutover meant adding `mainstagestudio.co.uk` + `www` to `mss-static`'s Domain Management (Cloudways re-routes nginx by hostname immediately, no propagation wait), issuing a fresh Let's Encrypt certificate for both hostnames on that app (the naked domain had no matching cert for a few minutes and would have failed HTTPS entirely), then deleting the domain from the WordPress app's Domain Management so it can't reclaim the vhost on a future WP-side config regen. WordPress (`mainstagestudio` app) is now reachable only at its temporary Cloudways URL — not deleted, kept as a fallback. Verified end-to-end post-cutover: apex and `www` both 200 over valid HTTPS, `/work/puremed/`, `/work/sable/`, `/work/sable/site/` all 200 with no redirect, cert subject `CN=mainstagestudio.co.uk` valid to 5 Nov 2026. |
| 7 August 2026 (fourth pass) | Gap band regenerated at its real shape, halved in height; What we do intro unstacked | Founder on the cropped band: visibly zoomed and too thick. Correct, and the cause is structural, not a crop setting. A close-up stays a close-up whatever you trim off it, so the fix was a new generation composed **for** a wide band rather than cut down into one. Seedream 4.5, 21:9 at 6048x2592, three matte board slabs in a low horizontal line, camera far back and near surface level, generous empty ground, brief grounded in the mark's geometry (near-black leads, terracotta, pale sand) and the palette from `mss-brand-identity.md`. Three variants: one put the slab band too high with a slab running out of frame, one hallucinated a trade-show background with people, purple signage and lettering (all three explicitly excluded in the brief), one landed. Production crop is 6:1 at 3000x500; the band went from 3:1/416px to 6:1/208px at a 1440 viewport, roughly the halving asked for, and opens to 4:1 below md because at 6:1 a 342px phone gets a 57px strip. Raw generation, brief and rejection notes at `01_mss/assets/generated/scenes/`. **Bug this exposed:** the anchor parallax used a fixed 12px drift, safe inside the 8% overhang of a 668px portrait and not inside a 208px band, where it would have shown parchment at the edges. Range is now derived from each element's own height. **What we do intro, same session, same defect family:** heading capped at 14ch set in three lines down the left edge with the standfirst stacked under it at 52ch, leaving the whole right half of the container empty. Heading and standfirst now sit side by side on a 1.35fr/1fr split with `items-end`, so they share a baseline; the heading breaks after "and" at its full 72px. |
| 7 August 2026 (third pass) | The Gap and How it works anchors rebuilt; founder called the whitespace unintentional and was right | Founder's read on The Gap: heading should be two lines, image should run the width of the section, the space across the page does not look deliberate. Measured before agreeing: at 64px "A launch is a moment." needs 539px and the `1fr` column at a 1440 viewport is 480px, so capped at `max-width:11ch` the claim was setting in **four** lines inside a box 336px wide in a 480px column. Two lines was not reachable at any sane size inside that column, so the heading moved out of it and spans the container; it now breaks once, where the sentence breaks, at the same 64px. The two paragraphs went side by side beneath it rather than leaving an empty left cell. The anchor became a full-width 3:1 band (2:1 below md) and gives the section a floor: the largest void was not the one the founder pointed at but the ~420px under the right column, caused by a 293px-tall image making the left column taller, which no side-by-side arrangement fixes. Asset is a new `gap-anchor-wide.png`, a 1728x576 band cropped from the same render across the point where all three forms meet, cropped in the file rather than with `object-fit` so the served bytes match the pixels shown. **Partial challenge, taken:** full width, but letterboxed, not a hero. Work is the only section where a photograph should lead, and the section's own brief calls this image punctuation. **Same defect fixed unprompted two sections down:** `hiw-anchor` was also 220px inside a 501px column with a column-height void beside it. It now fills the column and stays portrait, because that composition is vertical. Left and right columns of How it works now measure 786px each instead of 786 against 293. |
| 7 August 2026 (later same day) | Sable's own site ported to `/work/sable/site`, so the case study's "enter" works | The closing beat only lands if there is somewhere to walk into. `01_mss/portfolio/sable/web/index.html` is now a real route. Same fixes as the case study port: Tailwind built rather than CDN, Playfair Display and DM Sans self-hosted (DM Sans 400 added), album art through Astro's image pipeline. The hero film was a **27MB** mp4 with `preload="auto"` and no poster, so the first screen was black until it buffered; now 3.1MB VP9 with a 3.6MB H.264 fallback and a 12KB poster, and the 15.04s cut points the choreography depends on are unchanged. **Bug found in my own port and fixed:** an unlayered `.cta { color: inherit; font: inherit }` outranked `@layer utilities`, so `text-bone` and `text-gold` lost and every CTA on the page rendered in the body colour, near-black on near-black. Narrowed to `font-family: inherit`. **Other fixes:** nav scrim so the bone links survive the lit-studio opening frames of the film; bottom scrim behind the wordmark for the same reason; `updateNav()` now runs on load (a reload partway down the page left the nav transparent); overlays get `inert`, focus moved in and returned, previously neither; ticket buttons were hover-only, so they did not exist for keyboard users; the connect form signalled a bad address with a border colour and nothing else; the mobile panel stayed stranded when the viewport widened past the breakpoint. Two controls with nothing behind them ("stream", "stream free on sable") became buttons rather than `href="#"` links and say so when pressed. The page is `noindex`: an invented artist's site ranking on its own, detached from the case study that frames it, would read as a real one. A fixed return bar labels it a prototype and links back both ways. **Homepage nav, separate founder note the same session:** the desktop nav switched to the hamburger at `md` (768px), but the wordmark, four links and the CTA need about 880px on one line, so between 768 and 1024 every label wrapped onto two lines inside its own flex item. Breakpoint moved to `lg`, labels set `whitespace-nowrap`, and the same stranded-menu-on-resize fix applied here too. |
| 7 August 2026 | Work section rebuilt to three engagements; Sable case study ported into the Astro site | The offer now reads as presence **and** systems, and a single PureMed card was carrying the whole of it. Restored the pre-Astro Work layout the founder asked for: feature card, then a two-up row, with the two-row Sector/Scope meta table on every card. **Sable** now lives at `/work/sable` as a real Astro page rather than a standalone file in `01_mss/portfolio/`. The port keeps the design language and fixes what the standalone version could not do: Tailwind is built rather than pulled from a CDN script, Playfair Display 400 and DM Sans 300 are self-hosted (page-local `@font-face`, so the homepage does not pay for them), images go through Astro's pipeline (five PNGs over 1.5MB, one at 5.6MB, now responsive WebP), the six near-identical IntersectionObservers collapse to two, and the page gains reduced-motion handling, focus-visible states, a skip link and JS-gated reveals (it previously rendered blank without JS). The 15s hero film is embedded for the first time in the section that argues for it, re-encoded to 1.6MB WebM / 2.1MB MP4 with a poster and `preload="none"`. The nav's three dead `href="#"` links became one working back-link. **Two honest corrections:** the closing "enter" CTA opened Sable's own prototype site, which is not published, so the beat now closes on the self-initiated framing and points at `/#contact`; and the 3:4 crop on the discovery image pair was cutting the top of both heads off. **Ayesha** returns as a card only, no link, no spotlight hover, status corrected from the old "visual assets incoming" note to reflect that brand strategy is delivered and the build follows. |
| 6 August 2026 | PureMed promoted to a full client engagement, and the case study re-based on the go-live site | Two founder instructions in one session. **The move:** `other-projects/puremed/` and its publish worktree `other-projects/puremed-publish/` moved to `02_clients/`, per the stream separation rule. PureMed keeps its own repo and deploy workflows, so both are gitignored by the Studio repo; the worktree was repaired with `git worktree repair` and the stale gitlink dropped from the workspace index. Path references rewritten in `scripts/content-clients.json` and the defaults in `stage-autotag.js`, `mss-loop2.js` and `mss-review.js`, plus SOPs and plan docs. `02_clients/puremed/CLAUDE.md` no longer calls PureMed a personal project, which closes review item C6. **The correction:** the go-live site is the two-page Astro build (Home, plus one Treatments page carrying eleven anchored sections the nav deep-links into), which is also what Nafisa signed off on Stage. The case study had been claiming "13 pages" and "one dedicated page and URL per treatment", shot from ten superseded standalone prototypes in `web/` that were explored and dropped. Decisions 02 and 04, the shipped fact, the "What shipped" headline and copy, and the reflection all corrected; screenshots re-taken from `site/dist` and now show Home, Treatments, all eleven sections and both pages on mobile. `tools/clips/shoot-site.js` rewritten to shoot the Astro build. Left open for the founder: the treatments hero says "6 Specialist Treatments" against eleven sections on the page. Full record in `01_mss/website/mss-site-ux-review.md`, third pass. |
| 4 August 2026 (later same day) | Repositioning v5 — Systems added as a second service line | Founder wants to leave the door open to building operational systems, not just presence/content, given the founder's actual career strength is regulated systems/product delivery, not marketing. Added **Systems**: config-driven, deterministic, potentially bespoke operational architecture for regulated firms (client/case journey design, policy-gate logic, onboarding workflow tooling), sold scoped-and-quoted per engagement rather than packaged, distinct from Presence's fixed-price trio. Sharpens the hero line ("a better system") from a metaphor for MSS's content pipeline into something literally buyable. Flagged risk: the founder's strongest possible proof asset for this line, his own wealth-onboarding journey architecture, sits inside his current employer's product domain and can never be shown, so Systems sells on credential alone for longer than Presence did; needs a scope-of-responsibility clause before it's actively sold given the higher stakes of a deterministic system bug versus a missed content post. Canon: `mss-positioning-v5-regulated-services.md` §5.2, §8 risks 5-6, §9 item 9. |
| 4 August 2026 | Repositioning v5 — regulated professional services, founder-credential-led; all eight §9 decisions locked | v4's clinic vertical bet (23 Jul, never ratified) reviewed and dropped: the founder had no personal track record in clinics and would be learning that sector's operating rhythm while selling into it, and the anonymised-sports flagship proved throughput, not the trustworthiness a clinic buyer actually needs. Replaced with a credibility-led bet built on the founder's own, publicly-checkable career (confirmed against both CVs in `workspace/resources/`, corrected mid-session from an initial overstatement of "a decade in wealth onboarding" to the accurate ~13 years across UK financial services, of which wealth-specific onboarding is only the current ~11-month chapter, plus ~10 years before that in broadcast technology). Verdicts taken on `mss-positioning-v5-regulated-services.md` §9: (1) vertical bet = regulated professional services, founder-credential-led; (2) ICP kept broad across regulated/trust-sensitive professional services, not narrowed to one sub-vertical; (3) flagship proof = founder's personal career, employer never named, FSC/PureMed stay secondary execution proof; (4) offer names = Launch/Presence Monthly/Campaign; (5) Presence Monthly band = £1,200–£1,800/mo founding-client rate for the first regulated-services client, moving to the standard £1,500–£3,000/mo Run band once proven, £2,000–£4,000+/mo aspirational ceiling thereafter (v4's assumed clinic-LTV figure doesn't transfer to this buyer set); (6) site register = credential-led, "Who's behind it" moves up ahead of "See the machine"; (7) Vera POC and its clinic-specific content model parked, the underlying Studio/Stage/compliance-lint pipeline capability still stands as general proof; (8) Layer 2/3 (self-serve tier, flagship-as-revenue) stay deferred. "Who's behind it" section copy drafted in `mss-positioning-v5-regulated-services.md` §10, pending founder review of the specific figures and the "I" vs "we" register choice. Next: review the draft copy, then redraft `mss-website-copy-v2.md` for the regulated-professional-services ICP. |
| 22 July 2026 | Cloudways/GitHub Actions deploy pipeline for the MSS site went live | Built out following PureMed's workflow template (already adapted into `.github/workflows/mss-site-staging.yml` + `mss-site-deploy.yml` in commit `717a3e6`, path-filtered to `01_mss/website/site/**`, staying in the monorepo rather than a new repo). Manual Cloudways steps completed: `mss-static` PHP app created on `mss-do-lon-01`; deploy key added via the server-level Master Credentials SSH panel (this Cloudways UI generation has no per-app public-key upload — only the server-wide master login supports key auth, confirming the original setup doc's approach); repo secrets set. Found and fixed a real rsync bug during verification: the webroot directory is owned by a different system user than the SSH deploy user, so `-a`'s default dir-time and permission sync failed with exit 23 even though every file transferred correctly — fixed with `--omit-dir-times --no-perms`, verified locally before pushing. First real deploy confirmed live at the temporary Cloudways URL after a manual cache purge (Varnish was serving a stale default page at `/` even after the real file existed underneath — same caching behavior documented in `mss-deployment-explained.md`). Staging subdomain deferred, not needed yet. `01_mss/strategy/mss-astro-cloudways-setup.md` still describes the master-credentials path correctly; no correction needed there. |
| 21 July 2026 | PureMed parked; MSS itself becomes the Cloudways/Astro deploy proof | PureMed engagement paused. The prior sequencing (PureMed proves Cloudways deploy → MSS cuts over) is no longer viable with PureMed parked. MSS homepage v2 (`01_mss/website/site`, committed 717a3e6) becomes the first real Astro-on-Cloudways deploy instead. Live copy checked: no PureMed track-record claims baked into the homepage, only a conditional/pending Work card gated on §9.6 + client consent — nothing to unwind. Open flag: the "self-service edits" honesty constraint (§8.4 of `mss-repositioning-plan.md`) was written assuming PureMed would run the first Stage sign-off; that proof still needs to happen somewhere before the claim ships, likely via MSS's own future edit cycles. |
| 16 July 2026 | Repositioning v3 — all nine §9 decisions locked | Founder verdicts taken on `mss-repositioning-plan.md` §9: (1) positioning = presence partner over AI consultancy; (2) proposition line = "You don't need a bigger team. You need a better system."; (3) client definition v3 = established SMBs + funded founders, run-rate problem; (4) offer names = Launch/Run, Campaign folded into Run; (5) retainer band raised to £1,500–£3,000/mo for new proposals (delivery system now proven, not speculative) — FSC's existing £400/wk left as-is, not renegotiated; (6) PureMed on-site lock reversed — becomes flagship proof once live, pending client comfort check; (7) MSS site cutover holds for the Astro rebuild rather than patching live Bricks; (8) old hero line retired from hero, kept in About; (9) context-file reconciliation (`mss-website-brief.md`, `mss-brand-identity.md`, tone-of-voice audience refs) approved to proceed now that 1–4 are locked. Next: reconcile context files, then start MSS homepage prototype v2 from `mss-website-copy-v2.md`. |
| 5 August 2026 | PureMed cleared as a Work case study | Copy review session: section 3 and 4 headings on `mss-website-copy-v2.md` reworked into `mss-website-copy-v3.md` ("Launched is not the same as alive" → "A launch is a moment. Running it is the job."; "Who's behind it" → "Running systems that couldn't afford to fail."), section 4's closing paragraph reworded off "your regulator" onto "frameworks"/"a compliant system," and section 5's Launch paragraph opener tightened. Separately, PureMed reinstated as a Work card: client comfort confirmed, founder has the green light to show the delivered PureMed build as a regulated-industry case study, with a dedicated PureMed case study page planned once the Work page exists rather than folding it into Vera. "PureMed on the MSS site" locked decision above updated accordingly. |
| 12 July 2026 | Repositioning v3 proposal drafted and added to Open Decisions | Site copy stale against the June pivot (Astro stack, PureMed full-solution scope, FSC campaign ops). Full capability audit, positioning recommendation (presence partner over AI consultancy), Launch/Run offer architecture, Phase 1/2 site map, and complete proposed copy written to `01_mss/strategy/mss-repositioning-plan.md` + `mss-website-copy-v2.md`. Nothing locked — awaiting founder verdicts on the plan's §9. |
| 10 July 2026 | FSC media-day talent pipeline (Thread 9) built + verified; three cross-thread decisions locked | (1) **The roster DB is a people DB**: `content/db/people/` with roles (fighter/presenter/ambassador/staff/coach/exec) supersedes both Thread 7b's planned `fighters/` and Thread 8's mooted `ambassadors.json` — one id space, built once (`scripts/content-people.js`; 44 people seeded from fights.json incl. tiers). (2) **Identity refs stay in `content/identity/`** — people records point at the store; the reference-image + confirmed-by discipline is unchanged, no migration. (3) **Appearance ≠ clip, proposal ≠ fact**: set-file appearances are only queryable once confirmed with checkable evidence (same identityRule bar); coverage reporting and clip-picking run on confirmed data only. Pipeline: `content-set.js` (scan/contact sheets/appearances), Footage Sets screen in the review app, media-day clip categories + `people[]` participants on library entries, `content-coverage.js` two-mode gap report. `_e2e-set.js` 54/54. Canon: `fightstar-championship/fsc-media-day-talent-plan.md` §5. |
| 4 July 2026 | FSC content engine POC pivoted: Kenu Suthakaran + deterministic new-signing archetype | Client pivot: Kenu Suthakaran just signed to FSC (signing already public), and the POC must prove a repeatable, deterministic pattern for any fighter signing — not a one-off event countdown. New campaign archetype (`content/config/archetypes/new-signing.json`): post-signature establish arc (who-is / craft / what-next at startDate + 0/c/2c cadence offsets), rule-based footage classification and once-per-campaign slot assignment, copy restricted to client-confirmed `verifiedFacts`; announcement-day post kept as unscheduled demo only. Shoaib Yousef / FSC 37 campaign (3 drafted posts) archived under `content/{campaign,posts,calendar}/archive/`. Kenu arc drafted same day: July calendar validated, 3 posts scaffolded via pipeline, slide specs + captions written (lint clean), scheduled 11/16/21 July — startDate provisional. Copy deepened after founder review ("felt rushed"): web research established sourced credibility (13 amateur wins, two amateur titles — his "Double Champion" vlog after beating Adam Raja at FSC32 — unbeaten pro, Tamil-heritage story per his Tamil Guardian interview; founder challenged the "FSC title fight" detail and it was correctly downgraded to unconfirmed: only search-AI summaries of paywalled Tapology pages claimed it); campaign gained a sourcedFacts tier (cited public facts usable in drafts, client confirms at Stage review) and captions were rewritten story-led per the channel's 100x story-over-action finding. Awaiting client: startDate confirmation + verifiedFacts (signing terms, record, results, nickname, next opponent — possibly Adam Raja per the Face To Face video). Kenu inventory at `content/analysis/fighter-kenu-suthakaran.md`. |
| 3 July 2026 | Delivery pipeline built out: PureMed Astro site, deploy workflows, Loop 2 | PureMed Astro project generated mechanically from the approved Stage prototypes (both pages verified text-identical, `data-stage-id` preserved: 33 home / 63 treatments; nav variants kept per-page as approved). GitHub Actions: merge-to-main → Cloudways staging, manual-confirm dispatch → production — PR review is the deploy gate; Loop 3 is retired as a concept, CI replaces it. `scripts/mss-loop2.js` written and verified end-to-end with fixture data (changed/flagged/approved decisions applied to Astro source on a `signoff/*` branch, icons preserved in CTAs). Remaining to activate: GitHub remote for the puremed repo, Cloudways static app, repo secrets — see `puremed/.github/DEPLOY.md`. Review-state schema in Loop 2 needs verifying against the first real sign-off. |
| 3 July 2026 | Log backfilled — Astro pivot and Bricks cancellation recorded; upkeep rule locked | Log found stale: the two largest June decisions existed only in git commits and Claude auto-memory while this file still asserted the Bricks stack. Backfilled from commit history and memory. New rule: Claude Code updates this log in-session whenever a decision locks (see Workspace Structure). |
| 3 July 2026 | Stale positioning corrected in `mss-brand-identity.md` and `copy-contexts.md` | Brand identity file still carried the retired client definition, retired core proposition ("You know what you want...") and retired values filter — all superseded 05 June. Copy-contexts worked example still said "creative founders." Corrected to locked positioning. Flagged item closed. Note: these two files are in the Claude Project knowledge set — re-upload to claude.ai project needed. |
| 3 July 2026 | Repo cleanup — Bricks-era docs archived | Seven build-era docs moved to `_archive/bricks-era/` (commit 0b8089d). CLAUDE.md stack section corrected. Legacy-scope banner added to `mss-production-ops.md`. temp_stuff dissolved: FSC plan moved to `other-projects/fsc/`, playbook source transcript to `01_mss/strategy/sources/`. Workspace root git-initialised covering `scripts/` and `other-projects/`. Duplicate `mss-loop1.sh` deleted (cron runs the Node version). |
| 27 June 2026 | Bricks Builder cancelled and refunded | Consequence of the Astro pivot. Lifetime licence refunded. Backfilled 3 July from auto-memory. |
| 25 June 2026 | Stack pivot: WordPress + Bricks → Astro static sites on Cloudways | Bricks produced silent platform bugs (header meta-key sync, global settings deletion, ~2KB MCP payload truncation), two-layer caching, LocalWP dependency, and non-deterministic MCP sessions. Astro gives deterministic build pass/fail, static output, no WordPress maintenance surface. Stage (Pi review tool) becomes the client review mechanism; PureMed is the first engagement on the new pipeline. MSS repositioning as an AI consultancy — the delivery pipeline itself becomes the offering. Backfilled 3 July from auto-memory and git history. |
| 23 June 2026 | DAM deprioritised | Compressed WebP assets are small enough to serve directly from Cloudways. Bunny.net account provisioned but parked. No active use. Revisit when a client requires significant asset management. |
| 22 June 2026 | Higgsfield asset generation skill added | New global skill for coordinated image and video asset generation. Integrates Higgsfield MCP into the client delivery workflow as a defined phase between design system and web build. |
| 21 June 2026 | Untracked working-tree content committed — Flagged item resolved | Five-category review session: both case-study.html files committed (MSS hero spacing fix, Sable scroll animation refactor and new "The Hero" section s04b); strategy docs (9 files) and 03_resources/assets (16 files) committed together; Apex FC brought under version control for the first time (83 files); Sable brand assets committed (16 files — sable-homepage-image-01.png and -02.png confirmed as actively referenced in case-study.html, not stray duplicates); 03_resources/tools committed (5 text/script files, Video Optimiser.app already excluded via *.app gitignore rule). Strategy folder count corrected: 13 files as of 19 June, 4 removed since then (not 2 as stated in the original Flagged entry), 9 current. |
| 20 June 2026 | Constraints template moved from Flagged to Locked | Flagged entry ("no template exists") was stale — template had been placed at `02_clients/_template/discovery/constraints.md` during the 12 June migration. Strategy-folder draft merged in: stale paths corrected, closing specificity-rule "Notes" section added. Draft deleted. |
| 20 June 2026 | `MIGRATION-INSTRUCTIONS.md` deleted | Point-in-time record of the 12 June drift-fix migration. Items 1–8 confirmed applied (or correctly relocated to strategy tier); items 9–11 were optional recommendations, not instructions. No ongoing reference value. |
| 20 June 2026 | `README.md` updated — Bricks skill session ordering and extension protocol added | Preserved from `Bricks_Workflow_User_Guide.docx` before deletion: session ordering (site-connection → html-importer → lowcode-skill) and extension protocol (new pattern → lowcode-skill CPL, new hosting quirk → site-connection troubleshooting, workflow change → html-importer phases). |
| 20 June 2026 | `Bricks_Workflow_User_Guide.docx` deleted — docs audit item 3 closed | Format violated markdown-only convention; content superseded by four-skill system. Session ordering and extension protocol extracted to README.md before deletion. |
| 20 June 2026 | Three HTML files in `01_mss/strategy/` removed — docs audit item 2 closed | Content audit confirmed extraction complete: architecture in `mss-system-architecture.md`, tooling rationale in new `mss-tooling-rationale.md`, go-live checklist gaps merged into `mss-go-live-checklist.md`. Operating manual never git-tracked, fully superseded by current context files. |
| 21 June 2026 | Div/Container contradiction resolved — Container locked as correct layout element | Investigated: build guide (last modified 14 June) predates the four-skill system (19–20 June). Live MSS homepage DB query confirmed 33 container elements and 2 div elements. All three skills agree on Container. Build guide checklist corrected; decision locked. The 2 remaining Div elements are empty atmospheric-image overlay elements — correct for that specific role. |
| 20 June 2026 | `mss-bricks-build-guide.md` / `mss-bricks-quick-fixes.md` diff run — files confirmed distinct, Div/Container contradiction surfaced | Build guide (workflow doc) and quick-fixes (symptom lookup) confirmed serve different purposes and should not be merged. New blocker: build guide says Div for layout wrappers, `bricks-mss-site-notes` skill says Container. Remains in Flagged. |
| 20 June 2026 | Higgsfield hero asset moved from Open to Locked | MSS hero asset generated and confirmed; identity was fully locked first, satisfying the original dependency |
| 20 June 2026 | Higgsfield subscription extended, recurring monthly review framing locked | Renewed 18 June, runs to 18 July 2026, on a month-by-month basis. Reframed from a one-off "review after month one" decision to a recurring monthly checkpoint |
| 20 June 2026 | Editorial section removed from Open Decisions | Confirmed not a near-term priority, no longer worth tracking as an open item |
| 20 June 2026 | Spend tracker reconciliation reclassified | Moved out of Open Decisions into a new Action Items section — it's a task, not a decision |
| 20 June 2026 | Professional services case study — blocker clarified | Now explicitly blocked on PureMed proving out a repeatable Bricks build pattern, rather than "no timeline set" |
| 20 June 2026 | Domain spelling corrected | `mainstagestudio.co.uk` confirmed as the correct, live domain. The previous `mainstagingstudio.co.uk` entry in this log was incorrect and has been struck through and corrected. |
| 20 June 2026 | Portfolio folder naming resolved | Workspace taxonomy confirmed: `01_mss/portfolio/` contains `apex-fc`, `mss`, `sable`. Removed from Flagged. |
| 20 June 2026 | Voss case study dropped | Founder confirmed this is not being pursued. Removed from tracking; would follow the standard case study guide fresh if picked up again in future. |
| 20 June 2026 | Sable case study status corrected | Built and live on WordPress as a hidden page, not yet publicly published. Blocked on formatting fixes. Previously grouped with Voss under a single ambiguous flag; now tracked separately. |
| 20 June 2026 | Ayesha engagement status updated | Brand strategy document delivered, currently with the client for review. Not being actively chased — deprioritised behind internal work and the PureMed build. |
| 20 June 2026 | Apex FC case study copy confirmed paused | Founder confirmed this is intentionally on hold, not drifting — may be revisited later, possibly as proof of capability on a larger site |
| 20 June 2026 | MSS case study rewrite — dependency added | On hold until the copy document is updated and tone of voice is confirmed reflective of current positioning |
| 19 June 2026 | Corrected false claim that the docs audit file "was never located on disk" | A prior session searched incorrectly and wrote a wrong conclusion into this log. Confirmed via `find` and `git log` that the file exists and has been tracked since commit b7e1e31. |
| 19 June 2026 | `mss-decisions-log-workflow.md` and `mss-studio-ops-checklist.md` created | README referenced both files but neither existed. Created to complete the audit. |
| 19 June 2026 | Two-`.claude`-folders flagged item resolved | `README.md` audit locked the distinction: repo `.claude/` for MSS-specific context, global `~/.claude/` for portable skills. |
| 19 June 2026 | This file rebuilt from the correct 05 June base | A stale 25 May fork had been worked on in a parallel session and was mistakenly treated as current. The 05 June repo version (`workspace/main-stage-studio/.claude`) confirmed as the real lineage. All changes since 05 June folded in: Studio 2 reversed, DAM and Bricks licence resolved, site confirmed live, contact form built, Sable's stale "placeholder" flag removed, Bricks skill system added, several new items flagged |
| 17 June 2026 | Studio 2 venture reversed, single studio confirmed | A sense check on 4 June raised the overhead problem at pre-revenue stage. Founder confirmed: one studio, PureMed and Locked In Learning stay personal projects, possible future surface on an MSS Work page only |
| 17 June 2026 | DAM resolved to Bunny.net | Account provisioned, closes the Cloudflare R2 vs Bunny.net open decision |
| 17 June 2026 | Bricks Builder licence resolved to lifetime | Purchased, closes the annual vs lifetime open decision |
| 17 June 2026 | Contact form resolved to native WordPress | Custom mu-plugin relaying through Google Workspace SMTP, closes the native vs third-party open decision. Frontend rendering flagged for confirmation |
| 19 June 2026 | Bricks skill system added — four skills replacing the single `bricks-html-importer` approach | Original skill was being violated in production (Code elements used for content) and mixed three concerns — connection, element/style reference, conversion workflow — that needed separating |
| 05 June 2026 | Client definition widened — "creative founders" retired | Case study audit revealed three distinct archetypes: founders, established businesses that have lost calibration, creatives at any stage. Common thread is the gap between what something is and how it shows up, not life stage |
| 05 June 2026 | Core proposition updated — "You know what you want. We know how to build it." retired | Too narrow. Assumed clarity on the client's side. New line: "Your vision doesn't need permission. It needs form." |
| 05 June 2026 | Values filter updated — "founders" removed | Aligned to widened client definition. Filter is intent, not industry or life stage |
| 05 June 2026 | Non-conformist thread added to tone of voice | Voice recalibration: the confidence has a spine. A belief that the vision deserves to be heard. Runs underneath everything without appearing on the surface |
| 05 June 2026 | AI positioning updated | Reframed from "craft tool, not a shortcut" to "thinking partner." The judgment about what to do with the tools hasn't changed hands |
| 05 June 2026 | Work section structure locked | Homepage: curated highlights, two or three case studies. Dedicated Work page for full portfolio. PureMed off site entirely |
| 05 June 2026 | Hero copy and about section copy updated | Full rewrite in session. Hero proposition line, body copy, and about section all updated |
| 02 June 2026 | Tone of voice updated — empathy layer added to brand character | Voice recalibration: same rigour, more warmth |
| 02 June 2026 | Copy contexts updated — First Impression and Discovery Framing revised, Case Study and Editorial guidance updated | Aligned to updated tone of voice |
| 02 June 2026 | Case study framing page locked as structural approach | Confirmed in session — framing page precedes full case study write-up |
| 02 June 2026 | Studio voice in one line updated | Was "Quietly confident. Doesn't need to prove anything." Now "Quietly confident, and genuinely empathetic." |
| 25 May 2026 | Locked In Learning migrated out of MSS scope | Doesn't fit creative founder proposition — moved to Studio 2 (later reversed 17 June) |
| 25 May 2026 | Decisions log created | Review cycle — consolidating locked decisions from brand identity, tone of voice, copywriting skill, and website brief sessions |
| 24 May 2026 | Brand identity, tone of voice, copywriting skill — all updated | Review cycle — full rebuild of brand and voice layer |
| 24 May 2026 | Website brief — updated | Copy direction added; stack and code principles confirmed |
